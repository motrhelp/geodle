// const countryList = require('../data/CountryList');

// countryList.forEach(country => console.log(element));
// test("Check flag entity for each country", () => {
//     countryList.forEach(country => {
//         console.log(country.name);
//     });
//     });

